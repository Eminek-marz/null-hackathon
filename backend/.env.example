PORT=3001
CORS_ORIGIN=http://localhost:5173

# Optional: persist upload metadata + extracted text preview
# MONGODB_URI=mongodb://127.0.0.1:27017/studybuddy

# Optional: real AI (otherwise chat/simplify use shared mocks).
# Copy this file to `.env` and set GEMINI_API_KEY locally — never commit real keys.
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-1.5-flash
